# Clicky Local Gateway — Python dependencies
#
# Core server
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0
pydantic>=2.7.0

# Local transcription
faster-whisper>=1.0.3

# Future: encrypted SQLite persistence (Step 4)
# sqlcipher3>=0.5.0
cryptography>=42.0.0
